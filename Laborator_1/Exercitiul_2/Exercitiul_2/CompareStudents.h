#pragma once
#pragma disable(warning 4996);
#include "Student.h"
#include <cstring>

double compareByName(Student s1, Student s2);

double compareByMathGrade(Student, Student);

double compareByHistoryGrade(Student, Student);

double compareByEanglishGrade(Student, Student);

double compareByAverageGrade(Student, Student);